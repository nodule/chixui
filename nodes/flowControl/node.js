output.out =  new chix_ui.FlowControl(input.element, {
  styles: input.styles,
  config: input.config,
  pkgs: input.pkgs,
  state: input.state,
  flows: input.flows,
  nodeDefinitions: input.nodeDefinitions
});
